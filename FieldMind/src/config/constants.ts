import { CactusConfig } from 'cactus-react-native';

// Disable telemetry for privacy-first construction use case
CactusConfig.isTelemetryEnabled = false;

// Or enable with your token for analytics
// CactusConfig.telemetryToken = 'your-token';

